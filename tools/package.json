{
  "name": "vision-lab-baseline-tools",
  "private": true,
  "scripts": {
    "capture": "playwright test capture-baseline.spec.cjs --config=playwright.config.cjs",
    "phase2": "playwright test phase2-parity.spec.cjs --config=playwright.phase2.config.cjs",
    "phase4": "playwright test phase4-native-detector.spec.cjs --config=playwright.phase2.config.cjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
